<h2>Users</h2>

<% if @users.empty? %>
    <p>There are no users</p>
<% else %>
    <ul>
        <% @users.each do |user| %>
            <li><a href="<%= user_url(user) %>"><%= user.email %></li>
        <% end %>
    </ul>
<% end %>